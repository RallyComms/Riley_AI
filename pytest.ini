[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py

norecursedirs =
    .*
    .venv
    venv
    env
    build
    dist
    Riley_AI
    data
    artifacts
    .runs

addopts = -q --disable-warnings
