# Required for LLM classifier
OPENAI_API_KEY=api_key_here

# Model for classification (default: gpt-4o-mini). You can change it.
CLASSIFY_MODEL=gpt-4o-mini

# Embedding model (not used in Step 1, reserved for later)
EMBED_MODEL=text-embedding-3-large
